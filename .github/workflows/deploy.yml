name: Deploy London Game
on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # This step ensures the secrets actually exist before trying to use them
      - name: Verify Secrets
        run: |
          if [ -z "${{ secrets.MAPS_API_KEY }}" ]; then echo "SECRET MAPS_API_KEY IS MISSING"; exit 1; fi
          if [ -z "${{ secrets.MAPS_ID }}" ]; then echo "SECRET MAPS_ID IS MISSING"; exit 1; fi

      - name: Inject API Keys
        run: |
          # We use | as a delimiter in sed to avoid issues with special characters in keys
          sed -i "s|YOUR_API_KEY_HERE|${{ secrets.MAPS_API_KEY }}|g" index.html
          sed -i "s|YOUR_MAP_ID_HERE|${{ secrets.MAPS_ID }}|g" index.html

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: .
          branch: gh-pages
